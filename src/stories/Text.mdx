import { Meta, Typeset } from "@storybook/blocks";
import { typography, typographyWeight, } from '@/tokens/typography';
import {TYPOGRAPHY_SAMPLE_TEXT} from '@/constants';


<Meta title="components/Text" />
 
# Text (Typography)
 
## Heading - normal
<Typeset
  fontSizes={Object.entries(typography)
      .filter(([key]) => key.includes('heading'))
      .map(([, value]) => value).reverse()}
  fontWeight={typographyWeight.normal}
  sampleText={TYPOGRAPHY_SAMPLE_TEXT}
  fontFamily={typography.primary}
/>

## Heading - bold
<Typeset
  fontSizes={Object.entries(typography)
      .filter(([key]) => key.includes('heading'))
      .map(([, value]) => value).reverse()}
  fontWeight={typographyWeight.bold}
  sampleText={TYPOGRAPHY_SAMPLE_TEXT}
  fontFamily={typography.primary}
/>

## Heading - bright (light mode)
### light mode
<div className='light-text-shadow'>
  <Typeset
    fontSizes={Object.entries(typography)
        .filter(([key]) => key.includes('heading'))
        .map(([, value]) => value).reverse()}
    fontWeight={typographyWeight.thin}
    sampleText={TYPOGRAPHY_SAMPLE_TEXT}
    fontFamily={typography.primary}
  />
</div>
### dark mode
<div className='dark-text-shadow'>
  <Typeset
    fontSizes={Object.entries(typography)
        .filter(([key]) => key.includes('heading'))
        .map(([, value]) => value).reverse()}
    fontWeight={typographyWeight.thin}
    sampleText={TYPOGRAPHY_SAMPLE_TEXT}
    fontFamily={typography.primary}
  />
</div>

## Body - normal
<Typeset
  fontSizes={Object.entries(typography)
      .filter(([key]) => key.includes('body'))
      .map(([, value]) => value)}
  fontWeight={typographyWeight.normal}
  sampleText={TYPOGRAPHY_SAMPLE_TEXT}
  fontFamily={typography.primary}
/>

## Body - bold
<Typeset
  fontSizes={Object.entries(typography)
      .filter(([key]) => key.includes('body'))
      .map(([, value]) => value)}
  fontWeight={typographyWeight.bold}
  sampleText={TYPOGRAPHY_SAMPLE_TEXT}
  fontFamily={typography.primary}
/>


## Body - bright
### light mode
<div className='light-text-shadow'>
  <Typeset
    fontSizes={Object.entries(typography)
        .filter(([key]) => key.includes('body'))
        .map(([, value]) => value)}
    fontWeight={typographyWeight.thin}
    sampleText={TYPOGRAPHY_SAMPLE_TEXT}
    fontFamily={typography.primary}
  />
</div>
### dark mode
<div className='dark-text-shadow'>
  <Typeset
    fontSizes={Object.entries(typography)
        .filter(([key]) => key.includes('body'))
        .map(([, value]) => value)}
    fontWeight={typographyWeight.thin}
    sampleText={TYPOGRAPHY_SAMPLE_TEXT}
    fontFamily={typography.primary}
  />
</div>